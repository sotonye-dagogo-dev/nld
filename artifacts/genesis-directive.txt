ai-system must be bootstrapped
Client needs webapp that primarily satisfies these two conditions for the MVP
- The App Will Host The Devotionals.
- The Devotionals Will Also  Be  Paid  For In The App for users to access them
What I have in mind is a platform where authentication isn’t used for the mvp unless to access the admin panel, which is primarily for upload of Devotionals for now and also for seeing record of what goes on in the platform. As for main platform flow, I want a simple system for now because there’ll be features to add later and likely integration into a larger project that will deprecate this standalone tool (important context to note in planning and development as it needs to be easily integratable)
No need for large scale auth (aforementioned larger project will handle that and I don’t want scattered databases) but user emails will come into play, let me try to describe what I’m thinking. Users can browse uploaded Devotionals, a dedicated page for a devotional can be navigated to, user can purchase devotional (or rather access to it). In order to make purchase paystack will be connected, user also enters email. so they go through paystack thing and all and receive email from paystack but also from the platform (will likely use resend for mail) and mail from platform will include access password that will be generated somehow from the paystack transaction and all. user hasn’t fully stated how they want access given, whether it’s payment for each devotional and one-time payment gives full access for the month or for a time duration, but our system standard of having config-driven/metadata-driven admin-configurable programming and systems should help with that, such that on the admin panel all that can be configured and wired into platform behaviors. client also hasn’t specified if they want to restrict screenshots, screen capturing of any sort and/or retrieval of the asset from the platform (even from dev tools if possible) or not but I’d assume they’d prefer that so we want that as default behaviour and of course have it admin configurable
then record of all done on platform needs to be taken as well, full audit trails for asset uploads, user payments and access with emails captured too, possibly platform interactions like visits, counting of opeNing of Devotionals, platform visits, devotional page visits, etc. tech stack will likely be NextJS (maintain config driven approach with globally-defined universal components and wrappers and metadata driven programming with lists and arrays used and all, as stated in engineering standards) with tailwind (maintain config driven approach with styling done in global file and classes called from there), typescript (maintain config driven approach with globally defined types and interfaces and have the global file injected into typescript such that they don’t have to be imported in each file), drizzle with Supabase, deployed on vercel, paystack and resend for email, should also be PWA
client sent some vibecoded work and all that extends beyond the mvp and that’s what’s in the zip file in artifact but I don’t care much for it, still adding in case you get some context, also added the word file with their abstract requirements (some of which go beyond mvp) still just for context, because as always I want rich documentation and the project plan essentially should capture the future features that go beyond the mvp. feel free also to drop suggestions for this mvp and all
